# Hostname
string hostname

# UWB ID (if UWB attached)
string uwb_id

# Name of carrier robot
string robot_name

# Key of pose graph node after drop
uint64 pose_graph_key

# Relative pose to the pose graph node
geometry_msgs/Pose relative_pose
